# Local dev — Vite proxies /api to backend (leave empty or use /api)
VITE_API_URL=

# Production — only if API is on a different domain than the frontend
# VITE_API_URL=https://your-api.example.com/api
